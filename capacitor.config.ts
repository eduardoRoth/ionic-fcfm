import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.eduardoroth.fcfm',
  appName: 'ionic-fcfm',
  webDir: 'dist/ionic-fcfm/browser'
};

export default config;
